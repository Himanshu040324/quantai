[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
markers =
    network: makes real external network calls (yfinance/RSS) - excluded by default, run explicitly with `pytest -m network`
addopts = -m "not network"