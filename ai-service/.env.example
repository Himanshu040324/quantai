# MongoDB (same Atlas cluster as backend/, separate driver — Motor, not Mongoose)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net
MONGODB_DB_NAME=quantai

# Environment
ENV=development